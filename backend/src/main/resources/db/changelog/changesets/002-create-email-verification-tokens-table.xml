<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="002-create-email-verification-tokens-table" author="andrii" dbms="postgresql">
        <sql>
            CREATE TABLE vaultnote.email_verification_tokens (
                id BIGINT GENERATED BY DEFAULT AS IDENTITY,
                user_id BIGINT NOT NULL,
                token_hash VARCHAR(64) NOT NULL,
                expires_at TIMESTAMP WITH TIME ZONE NOT NULL,
                used_at TIMESTAMP WITH TIME ZONE,
                created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
                CONSTRAINT pk_email_verification_tokens PRIMARY KEY (id),
                CONSTRAINT uk_email_verification_tokens_hash UNIQUE (token_hash),
                CONSTRAINT fk_email_verification_tokens_user
                    FOREIGN KEY (user_id)
                    REFERENCES vaultnote.users (id)
                    ON DELETE CASCADE,
                CONSTRAINT ck_email_verification_tokens_expiry
                    CHECK (expires_at &gt; created_at)
            );

            CREATE INDEX ix_email_verification_tokens_user_id
                ON vaultnote.email_verification_tokens (user_id);
        </sql>
    </changeSet>

</databaseChangeLog>
