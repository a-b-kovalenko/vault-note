<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="005-create-notes-table" author="andrii" dbms="postgresql">
        <sql>
            CREATE TABLE vaultnote.notes (
                id BIGINT GENERATED BY DEFAULT AS IDENTITY,
                owner_id BIGINT NOT NULL,
                title VARCHAR(200) NOT NULL,
                content VARCHAR(20000) NOT NULL,
                version BIGINT NOT NULL DEFAULT 0,
                created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
                updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT CURRENT_TIMESTAMP,
                CONSTRAINT pk_notes PRIMARY KEY (id),
                CONSTRAINT fk_notes_owner
                    FOREIGN KEY (owner_id)
                    REFERENCES vaultnote.users (id)
                    ON DELETE CASCADE,
                CONSTRAINT ck_notes_title_not_blank
                    CHECK (btrim(title) &lt;&gt; '')
            );

            CREATE INDEX ix_notes_owner_id
                ON vaultnote.notes (owner_id);
        </sql>
    </changeSet>

</databaseChangeLog>
