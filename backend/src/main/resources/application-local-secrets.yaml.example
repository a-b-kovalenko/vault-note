# Copy this file to application-local-secrets.yaml and fill the local-only
# values. Generate the JWT secret with: openssl rand -base64 32
app:
  jwt:
    secret:

spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id:
            client-secret:
