= ADR 013: CSRF and CORS for browser authentication
:status: accepted
:date: 2026-08-11

== Context

VaultNote will be called by an Angular application hosted at a different
origin from the Spring Boot API. The refresh token is delivered in an
`HttpOnly` cookie, so the browser can attach it automatically to refresh and
logout requests. CORS must allow the trusted frontend to call the API, while
CSRF must prevent another site from driving those cookie-backed operations.

The API also has public registration and email-verification endpoints. They
must remain usable before a browser session exists and therefore need an
explicit boundary in the initial CSRF configuration.

== Decision

Use an explicit, environment-driven CORS allowlist:

* default the allowed origin to `http://localhost:4200`;
* allow credentials because the refresh token is in a cookie;
* allow the API methods and headers required by the Angular client, including
  `Authorization`, `Content-Type`, and `X-XSRF-TOKEN`;
* reject origins outside the allowlist; never combine credentials with `*`.

Use Spring Security's cookie-based SPA CSRF flow:

* expose `GET /csrf` as a public endpoint;
* store the raw CSRF token in a non-`HttpOnly` `XSRF-TOKEN` cookie so Angular
  can read it;
* require the same value in the `X-XSRF-TOKEN` request header;
* protect state-changing requests by default, including login, refresh, and
  logout;
* explicitly exclude registration and email verification until those public
  flows receive their own browser CSRF integration.

The term `state-changing` refers to an operation with a side effect, not to
the creation of a Spring HTTP session. In this application login creates a
refresh-token record and cookie, refresh rotates that record, and logout
revokes it and clears the cookie. These operations remain CSRF-protected under
`SessionCreationPolicy.STATELESS`.

Use `SameSite=Lax` for the local CSRF cookie and keep the CORS origin in
`VAULTNOTE_CORS_ALLOWED_ORIGINS`. Production deployments must provide their
real frontend origin and review cookie `Secure` and `SameSite` settings.

== Consequences

* A trusted Angular client can perform credentialed cross-origin requests.
* A foreign origin cannot use CORS to read VaultNote responses and cannot
  satisfy the CSRF header check for cookie-backed mutation requests.
* The CSRF cookie is intentionally readable by frontend JavaScript; it is not
  an authentication credential and is paired with the server-side comparison.
* Bruno and curl do not implement browser CORS automatically. Manual clients
  must first obtain `/csrf`, preserve the `XSRF-TOKEN` cookie, and send its
  value in `X-XSRF-TOKEN` for login, refresh, and logout.
* Rate limiting, authentication audit events, and production cookie policy are
  separate hardening decisions.

== Status

Accepted and implemented by `CorsConfiguration`, `CsrfConfiguration`,
`CsrfController`, `SecurityConfig`, and `CsrfCorsIntegrationTest`.
