name: vault-note

services:
  mailpit:
    image: axllent/mailpit:v1.27.4
    restart: unless-stopped
    ports:
      - "${VAULTNOTE_MAILPIT_SMTP_PORT:-1025}:1025"
      - "${VAULTNOTE_MAILPIT_UI_PORT:-8025}:8025"
